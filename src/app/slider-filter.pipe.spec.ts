import { SliderFilterPipe } from './slider-filter.pipe';

describe('SliderFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SliderFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
